# TraceDev · 设计规范文档

> 先完成设计说明文档，再落地开发页面。用标准化文档锁定全部视觉边界，从根源杜绝 AI 模板化陷阱。

---

## 一、站点定位与表达内核

| 维度 | 定义 |
|------|------|
| **目标用户** | 嵌入式/单片机学习者、电子竞赛参与者、理工科大学生 |
| **核心用途** | 技术教程视频导航 + 开发资源（代码/PCB/APK）下载 + 咸鱼店铺引流 |
| **气质关键词** | 极客克制 · 信息清晰 · 专业可信 · 不喧宾夺主 |
| **一句话定位** | 一个让嵌入式开发者快速找到资源的低调工具站 |

### 拒绝什么（避坑清单）
- ❌ 拒绝霓虹赛博朋克风——用户是工程师，不是游戏玩家
- ❌ 拒绝过度动效——卡片 hover 只做微抬升，不做旋转/缩放/粒子
- ❌ 拒绝信息密度过低——不用大面积空白占满首屏
- ❌ 拒绝模板感——不用千篇一律的 hero gradient + 三个 feature card
- ❌ 拒绝拥挤布局——卡片之间保持呼吸感

---

## 二、信息架构 & 页面结构

```
┌─────────────────────────────────────────────┐
│  Navbar (fixed)                              │
│  [Logo] [视频] [店铺] [下载] [FAQ] [关于] [🌙] │
├─────────────────────────────────────────────┤
│  Tab Panel (动态切换，非页面跳转)              │
│                                              │
│  📺 教学视频    → 视频卡片网格 (3列)           │
│  🏪 咸鱼店铺    → 商品卡片 (居中flex wrap)     │
│  📦 资料下载    → 分类下拉 + 资源卡片网格       │
│  ❓ 疑难解答    → FAQ 手风琴列表               │
│  👤 关于作者    → 个人信息 + 技能条             │
│                                              │
├─────────────────────────────────────────────┤
│  Footer: Copyright · 备案号 · 邮箱 · 访问统计  │
└─────────────────────────────────────────────┘
```

### 每个面板的结构规范
- **面板标题**：h2, 24px/700, 居中或左对齐
- **面板描述**：p, 14px/secondary color, 标题下方 8px
- **内容区**：与标题间距 32px
- **空状态**：居中图标 + 主文案 + 副文案，icon 48px 灰色

---

## 三、精细化视觉规范

### 3.1 色彩系统

```
主题：深色优先（工程师偏好），支持浅色切换

深色主题：
  背景色阶:  #08080d (body) → #111118 (card) → #18181f (elevated)
  文字色阶:  #f1f5f9 (primary) → #94a3b8 (secondary) → #64748b (muted)
  边框色阶:  #1e293b (default) → #334155 (hover) → rgba(255,255,255,0.06) (subtle)

  品牌主色:  #00b4d8 (accent blue) — 仅用于选中态、链接、CTA按钮
  品牌辅色:  #6366f1 (indigo) — 仅用于渐变点缀、技能条
  功能色:
    成功: #22c55e (下载/复制成功)
    警告: #f59e0b
    错误: #ef4444

浅色主题：
  背景色阶:  #f8fafc (body) → #ffffff (card) → #f1f5f9 (elevated)
  文字色阶:  #0f172a (primary) → #475569 (secondary) → #94a3b8 (muted)
  边框色阶:  #e2e8f0 (default) → #cbd5e1 (hover)

用色铁律：
  - 品牌色出现频率：每个卡片最多 1 处（CTA按钮或图标）
  - 正文绝不用品牌色
  - 渐变仅用于技能条和卡片的顶部装饰线（1px），不用于大面积背景
```

### 3.2 字体层级

```
字体族:
  中文正文: Inter → system-ui → sans-serif
  代码/数字: JetBrains Mono → monospace

层级表（桌面端）:
  h2 面板标题:   24px / 700 / letter-spacing: -0.02em / primary
  h3 卡片标题:   15px / 600 / primary
  正文大:        14px / 400 / secondary (卡片描述、FAQ回答)
  正文小:        13px / 400 / muted (日期、标签、辅助信息)
  微文案:        12px / 400 / dim (页脚、时间戳)
  代码:          13px / 400 / JetBrains Mono (链接、密码、命令)

行高规范:
  标题: 1.3
  正文: 1.6
  微文案: 1.4
```

### 3.3 间距与圆角

```
间距尺度（基于 4px）:
  xs: 4px   (图标与文字间距)
  sm: 8px   (卡片内元素间距)
  md: 12px  (同类元素间距)
  lg: 16px  (区块内边距)
  xl: 24px  (卡片间距 grid gap)
  2xl: 32px (标题与内容间距)
  3xl: 48px (面板上下内边距)

圆角尺度:
  sm: 6px   (标签、小按钮、代码块)
  md: 10px  (卡片、输入框、FAQ项)
  lg: 14px  (模态框、大卡片)
  full: 50% (头像、播放按钮)

卡片统一圆角: 10px
按钮统一圆角: 8px
```

### 3.4 卡片设计规范

```
通用卡片 = 背景色(bg-card) + 1px边框(border-card) + 10px圆角

hover 态:
  transform: translateY(-2px)        ← 仅微抬，不做大位移
  border-color → border-hover
  box-shadow: 0 4px 20px rgba(0,0,0,0.1)  ← 仅深色模式有阴影
  transition: 0.25s ease             ← 统一过渡时间

active 态:
  transform: translateY(0)
  transition: 0.1s ease

资源卡片额外规范:
  - 顶部 1px 渐变装饰线（hover 时显现，非 hover 隐藏）
  - 不做 glow 旋转边框动画（太花哨）
  - 图标统一 40x40 圆角方块，品牌色半透明底
```

### 3.5 动效规范

```
过渡时间统一:
  微交互(hover/active): 0.2s ease
  卡片/面板切换:        0.3s ease
  模态框弹出:           0.25s ease-out (scale + fade)
  主题切换背景色:       0.35s ease

动效强度:
  - 卡片 hover: translateY(-2px) — 仅位移，不缩放
  - FAQ 展开: max-height 过渡
  - 面板切换: fadeInUp (opacity + translateY 20px)
  - 技能条: 仅首次进入可视区时播放，不循环

禁止:
  ❌ 禁止无限循环动画（除主题 toggle 图标旋转）
  ❌ 禁止 3D transform
  ❌ 禁止 hover 时 scale > 1.02
  ❌ 禁止超过 2 个同时进行的动画
```

### 3.6 背景环境

```
深色模式背景:
  - body 底色: #08080d (纯深色)
  - 网格点阵: 64px 间距, rgba(255,255,255,0.03) — 非常微妙
  - 环境光晕: 保留 2 个固定光晕 (蓝/紫)，但不透明度降低到 0.06

浅色模式背景:
  - body 底色: #f8fafc
  - 网格点阵: rgba(0,0,0,0.03)
  - 环境光晕: 不透明度降低到 0.03
```

---

## 四、组件状态完整定义

每个交互组件必须覆盖以下状态：

| 状态 | 处理方式 |
|------|----------|
| **rest** | 默认样式，边框 default |
| **hover** | 边框变亮，微抬升 2px |
| **focus-visible** | 品牌色焦点环 (2px) |
| **active** | 回弹到位移 0，0.1s |
| **disabled** | opacity: 0.4，cursor: not-allowed |
| **loading** | 骨架屏占位，不显示空白区 |
| **empty** | 居中空状态插画 + 引导文案 |
| **error** | 红色边框 + 错误提示文字 |

---

## 五、内容策略

### 空状态文案规范
```
无视频:  "📺 教程筹备中" / "正在录制第一批教学视频，敬请期待…"
无商品:  "🏪 商品上架中" / "更多开发板和学习套件即将上架"
无资源:  "📦 暂无该类资源" / "该分类下还没有资源，请查看其他分类"
无FAQ:   "❓ 暂无常见问题" / "如有疑问请通过邮箱联系作者"
```

### 按钮微文案规范
```
获取下载 → 打开百度网盘
直接下载 → 下载 APK (xx MB)
查看详情 → 去咸鱼查看
复制 → ✓ 已复制
```

---

## 六、技术约束

| 项目 | 选择 | 原因 |
|------|------|------|
| CSS 框架 | Tailwind CDN + 自定义 CSS 变量 | 快速开发，无需构建工具 |
| 图标 | Font Awesome 6 | 图标库完整，CDN 加载 |
| 字体 | Inter + JetBrains Mono (Google Fonts) | 免费、中文支持好 |
| 主题切换 | CSS 变量 + data-theme 属性 | 零 JS 依赖的主题系统 |
| 数据 | 静态 JS 对象 | 无需后端，方便替换 |
| 统计 | 百度统计 + 不蒜子 | 国内访问统计 |

---

## 七、优化优先级

| 优先级 | 类别 | 说明 |
|--------|------|------|
| P0 | 减少视觉噪音 | 去掉 card-glow 旋转动画，简化 hover 效果 |
| P0 | 统一间距/圆角 | 所有卡片统一 10px 圆角，间距统一到 4px 体系 |
| P1 | 补全面板标题 | 每个面板增加 h2 标题 + 描述段落 |
| P1 | 改善空状态 | 空状态设计更有温度感的插画和文案 |
| P2 | 优化色彩 | 品牌色使用更克制，减少 neon 感 |
| P2 | 内容真实化 | 补充真实的 FAQ 内容和关于作者信息 |
| P3 | 加载骨架屏 | 大型资源列表增加骨架屏 |
| P3 | 微交互打磨 | 复制按钮、下载按钮增加反馈细节 |
