# TraceDev · 工程资料站设计规范

> 当前规范以“工程资料归档站”为方向：信息优先、少卡片化、操作分级，避免生成式 AI 常见的胶囊按钮、发光渐变和悬浮卡片。

---

## 一、站点定位与表达内核

| 维度 | 定义 |
|------|------|
| **目标用户** | 嵌入式/单片机学习者、电子竞赛参与者、理工科大学生 |
| **核心用途** | 技术教程视频导航 + 开发资源（代码/PCB/APK）下载 + 咸鱼店铺引流 |
| **气质关键词** | 工程归档 · 信息清晰 · 专业可信 · 安静克制 |
| **一句话定位** | 一个让嵌入式开发者快速找到资源的低调工具站 |

### 拒绝什么（避坑清单）
- ❌ 拒绝霓虹赛博朋克风——用户是工程师，不是游戏玩家
- ❌ 拒绝过度动效——卡片 hover 只做微抬升，不做旋转/缩放/粒子
- ❌ 拒绝信息密度过低——不用大面积空白占满首屏
- ❌ 拒绝模板感——不用千篇一律的 hero gradient + 三个 feature card
- ❌ 拒绝所有内容都卡片化——优先使用列表、分隔线和元数据
- ❌ 拒绝胶囊按钮——按钮统一使用 4px 小圆角

---

## 二、信息架构 & 页面结构

```
┌─────────────────────────────────────────────┐
│  Navbar (fixed)                              │
│  [Logo] [视频] [店铺] [下载] [FAQ] [关于] [🌙] │
├─────────────────────────────────────────────┤
│  Tab Panel (动态切换，非页面跳转)              │
│                                              │
│  📺 教学视频    → 视频卡片网格 (3列)           │
│  🏪 咸鱼店铺    → 商品卡片 (居中flex wrap)     │
│  📦 资料下载    → 分类下拉 + 资源卡片网格       │
│  ❓ 疑难解答    → FAQ 手风琴列表               │
│  👤 关于作者    → 个人信息 + 技能条             │
│                                              │
├─────────────────────────────────────────────┤
│  Footer: Copyright · 备案号 · 邮箱 · 访问统计  │
└─────────────────────────────────────────────┘
```

### 每个面板的结构规范
- **面板标题**：h2, 24px/700, 居中或左对齐
- **面板描述**：p, 14px/secondary color, 标题下方 8px
- **内容区**：与标题间距 32px
- **空状态**：居中图标 + 主文案 + 副文案，icon 48px 灰色

---

## 三、精细化视觉规范

### 3.1 色彩系统

```
主题：浅色纸张风格为默认，支持深色切换

深色主题：
  背景色阶:  #191816 (body) → #211f1c (surface) → #2b2824 (muted)
  文字色阶:  #efebe3 (primary) → #c2bbb0 (secondary) → #8f887d (muted)
  边框色阶:  #3b3731 (default) → #5a534a (strong)

  品牌主色:  #cf7149 (brick orange) — 仅用于选中态、链接、主按钮
  功能色:
    成功: #22c55e (下载/复制成功)
    警告: #f59e0b
    错误: #ef4444

浅色主题：
  背景色阶:  #f3f0e9 (body) → #fbfaf7 (surface) → #ebe6dc (muted)
  文字色阶:  #242321 (primary) → #625f59 (secondary) → #8a857c (muted)
  边框色阶:  #d8d2c7 (default) → #aaa297 (strong)
  品牌主色:  #a44725，hover #84381f

用色铁律：
  - 品牌色出现频率：每个卡片最多 1 处（CTA按钮或图标）
  - 正文绝不用品牌色
  - 不使用装饰渐变、发光和卡片顶部彩线
```

### 3.2 字体层级

```
字体族:
  中文正文: Inter → system-ui → sans-serif
  代码/数字: JetBrains Mono → monospace

层级表（桌面端）:
  h2 面板标题:   24px / 700 / letter-spacing: -0.02em / primary
  h3 卡片标题:   15px / 600 / primary
  正文大:        14px / 400 / secondary (卡片描述、FAQ回答)
  正文小:        13px / 400 / muted (日期、标签、辅助信息)
  微文案:        12px / 400 / dim (页脚、时间戳)
  代码:          13px / 400 / JetBrains Mono (链接、密码、命令)

行高规范:
  标题: 1.3
  正文: 1.6
  微文案: 1.4
```

### 3.3 间距与圆角

```
间距尺度（基于 4px）:
  xs: 4px   (图标与文字间距)
  sm: 8px   (卡片内元素间距)
  md: 12px  (同类元素间距)
  lg: 16px  (区块内边距)
  xl: 24px  (卡片间距 grid gap)
  2xl: 32px (标题与内容间距)
  3xl: 48px (面板上下内边距)

圆角尺度:
  sm: 4px   (按钮、标签、输入框)
  md: 6px   (图片、复杂内容容器)
  lg: 8px   (模态框)
  full: 50% (头像、播放按钮)

普通按钮统一圆角: 4px
模态框统一圆角: 8px
```

### 3.4 卡片设计规范

```
普通资源、日志和 FAQ 不使用通用卡片，采用连续列表和横向分隔线。

hover 态:
  background → muted surface
  标题或链接颜色 → 品牌色
  不移动、不缩放、不加卡片阴影

active 态:
  transform: translateY(0)
  transition: 0.1s ease

资源列表规范:
  - 桌面端使用文件类型、名称说明、分类日期和操作四列
  - 移动端元数据折行，下载按钮保持直接可点
  - 文件图标使用 42x42 方形中性色底
```

### 3.5 动效规范

```
过渡时间统一:
  微交互(hover/active): 0.2s ease
  卡片/面板切换:        0.3s ease
  模态框弹出:           0.25s ease-out (scale + fade)
  主题切换背景色:       0.35s ease

动效强度:
  - 列表 hover: 仅背景颜色变化
  - FAQ 展开: max-height 过渡
  - 面板切换: opacity + translateY 4px

禁止:
  ❌ 禁止无限循环动画（除主题 toggle 图标旋转）
  ❌ 禁止 3D transform
  ❌ 禁止 hover 缩放和卡片抬升
  ❌ 禁止超过 2 个同时进行的动画
```

### 3.6 背景环境

```
深浅主题均使用纯色纸张背景，不使用点阵、光晕或渐变环境层。
```

---

## 四、组件状态完整定义

每个交互组件必须覆盖以下状态：

| 状态 | 处理方式 |
|------|----------|
| **rest** | 默认样式，边框 default |
| **hover** | 边框变亮，微抬升 2px |
| **focus-visible** | 品牌色焦点环 (2px) |
| **active** | 回弹到位移 0，0.1s |
| **disabled** | opacity: 0.4，cursor: not-allowed |
| **loading** | 骨架屏占位，不显示空白区 |
| **empty** | 居中空状态插画 + 引导文案 |
| **error** | 红色边框 + 错误提示文字 |

---

## 五、内容策略

### 空状态文案规范
```
无视频:  "📺 教程筹备中" / "正在录制第一批教学视频，敬请期待…"
无商品:  "🏪 商品上架中" / "更多开发板和学习套件即将上架"
无资源:  "📦 暂无该类资源" / "该分类下还没有资源，请查看其他分类"
无FAQ:   "❓ 暂无常见问题" / "如有疑问请通过邮箱联系作者"
```

### 按钮微文案规范
```
获取下载 → 打开百度网盘
直接下载 → 下载 APK (xx MB)
查看详情 → 去咸鱼查看
复制 → ✓ 已复制
```

---

## 六、技术约束

| 项目 | 选择 | 原因 |
|------|------|------|
| CSS 框架 | Tailwind CDN + 自定义 CSS 变量 | 快速开发，无需构建工具 |
| 图标 | Font Awesome 6 | 图标库完整，CDN 加载 |
| 字体 | Inter + JetBrains Mono (Google Fonts) | 免费、中文支持好 |
| 主题切换 | CSS 变量 + data-theme 属性 | 零 JS 依赖的主题系统 |
| 数据 | 静态 JS 对象 | 无需后端，方便替换 |
| 统计 | 百度统计 + 不蒜子 | 国内访问统计 |

---

## 七、优化优先级

| 优先级 | 类别 | 说明 |
|--------|------|------|
| P0 | 减少视觉噪音 | 去掉 card-glow 旋转动画，简化 hover 效果 |
| P0 | 统一间距/圆角 | 所有卡片统一 10px 圆角，间距统一到 4px 体系 |
| P1 | 补全面板标题 | 每个面板增加 h2 标题 + 描述段落 |
| P1 | 改善空状态 | 空状态设计更有温度感的插画和文案 |
| P2 | 优化色彩 | 品牌色使用更克制，减少 neon 感 |
| P2 | 内容真实化 | 补充真实的 FAQ 内容和关于作者信息 |
| P3 | 加载骨架屏 | 大型资源列表增加骨架屏 |
| P3 | 微交互打磨 | 复制按钮、下载按钮增加反馈细节 |
